import { useEffect, useState } from 'react';

const useCloudinaryUpload = (file, publicId) => {
    const [url, setUrl] = useState(null);
    const [error, setError] = useState(null);

    useEffect(() => {});
};

export default useCloudinaryUpload;
